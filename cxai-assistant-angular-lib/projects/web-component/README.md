# Web components bundle

This project builds web components bundle for use with legacy Accelerator addon - `cxaiaskproductaddon`.
Run `npm run build:web-component` from workspace root to create the bundle.

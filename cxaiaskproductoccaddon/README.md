### Legacy OCC AddOn Extension
Use this extension **only if you use legacy OCC addons** (`ycommercewebservices` instead of `commercewebservices`)
You need to install it as addon into your `ycommercewebservices` extension (same as `cmsoccaddon` etc.)

Otherwise ignore this extension and use `cxaiaskproductocc` instead